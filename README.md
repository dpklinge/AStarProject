# AStarProject
Tests are read from the TestFiles directory automatically; add a new test configuration and it will run.

To change solution maps, either modify the DefaultSolution.txt file, or modify the main function in the EightPuzzle class.